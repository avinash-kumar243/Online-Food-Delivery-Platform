spring:
  datasource:
    url: ${DB_URL:jdbc:h2:mem:cartdb}
    driver-class-name: org.h2.Driver
    username: ${H2_DB_USERNAME:sa}
    password: ${H2_DB_PASSWORD:}
  h2:
    console:
      enabled: true
      path: /h2-console
